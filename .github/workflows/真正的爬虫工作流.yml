name: 自动爬取直播源

on:
  schedule:
    - cron: '0 6,18 * * *'  # 每天6:00和18:00 UTC运行
  workflow_dispatch:        # 支持手动触发
  push:
    branches:
      - main
    paths:
      - 'crawler.py'

jobs:
  fetch-sources:
    name: 爬取直播源
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      - name: 运行爬虫
        run: |
          python crawler.py
      
      - name: 验证生成的文件
        run: |
          echo "📊 文件验证:"
          ls -lh data/
          echo ""
          echo "M3U文件行数: $(wc -l < data/result.m3u)"
          echo "TXT文件行数: $(wc -l < data/result.txt)"
          echo "JSON文件大小: $(wc -c < data/sources.json) 字节"
      
      - name: 配置Git用户信息
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Crawler"
      
      - name: 提交并推送
        run: |
          git add data/
          
          if git diff --quiet --cached; then
            echo "ℹ️ 没有新的更改"
          else
            git commit -m "🔄 自动爬取更新直播源 - $(date '+%Y-%m-%d %H:%M:%S UTC')"
            git push
            echo "✅ 更改已推送"
          fi
      
      - name: 完成
        run: |
          echo "=========================================="
          echo "✅ 直播源爬虫运行完成!"
          echo "=========================================="
          echo "📁 已生成文件:"
          echo "  - data/result.m3u"
          echo "  - data/result.txt"
          echo "  - data/sources.json"
          echo ""
          echo "🔗 TVBOX使用地址:"
          echo "  https://raw.githubusercontent.com/${{ github.repository }}/main/data/result.m3u"
          echo "=========================================="
